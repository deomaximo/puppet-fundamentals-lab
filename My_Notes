Getting 404 errors when running: sudo yum -y install nano git ntp
Had to run: 'sudo yum clean all' then 'sudo yum update all'

Pluralsight missing the https which is now required
sudo yum -y install https://yum.puppetlabs.com/puppetlabs-release-el-6.noarch.rpm
